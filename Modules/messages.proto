syntax = "proto3";

package waksage;

message Message {
    uint64 timestamp = 1;
    string sender = 2;
    string to = 3;
    string type = 4;
    string body = 5;
}

message Payload {
    Message message = 1;
}