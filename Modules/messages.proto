syntax = "proto3";

package waksage;

message MessageBody {
    string to = 1;
    string content = 2;
}

message Message {
    uint64 timestamp = 1;
    string sender = 2;
    MessageBody body = 3;
}

message Payload {
    Message message = 1;
}